import React from 'react'

export default class CoreValidator {
    get component() {return this.component}
    set component(v) {this.component = v}
    get message() {return this.message}
    set message(v) {this.message = v}
    get isValid() {

    }
}
